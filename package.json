{
  "name": "opening-trainer",
  "version": "0.1.0",
  "private": true,
  "description": "Personal chess opening trainer (Scotch as White, Elephant Gambit as Black). Static PWA, no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "serve": "python -m http.server 8765 --bind 127.0.0.1"
  }
}
