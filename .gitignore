# original delivery archive, already extracted
files.zip

# local tooling / secrets / data
.claude/settings.local.json
.env
*.local
data/
backups/
exports/

# OS / editor
Thumbs.db
.DS_Store

# any secret-looking file, whatever the extension
*.env
*.env.*
secret*

# third-party text harvested for research (public repo: never commit)
library/ideas-cache/
