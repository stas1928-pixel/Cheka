# Layout, running, and moving to another machine

Static site. No build step, no npm dependencies, no backend. Node is only
needed to run the tests; Python only to serve the folder locally.

## Files

```
index.html              the whole UI (home with swipe tabs, trainer + lines drawer, celebration layer)
manifest.webmanifest    PWA manifest ("Add to Home Screen")
icon.svg                app icon
js/app.js               DOM glue: home tabs, sessions, training loop (learn → from memory, parts of 6), plan panel, Watch + what-if, drawer, panels
js/repertoire.js        loads the generated data (notes and plans travel inside it)
js/names.js             human line names + family names shown in the app (test: no notation)
js/describe.js          plain-language move descriptions ("Knight takes the pawn on d4")
css/tokens.css          the palette (Onyx + ice family) and spacing/motion scale — the only place colours live
css/style.css           prototype base styles; overridden by the layers below
css/app-v2.css          pass 2 layer (buttons, chips, bars)
css/app-v3.css          pass 4 layer (home tabs, opening cards, progress, sheet, rings)
css/app-v5.css          pass 5 layer (sessions, plan panel, drawer, Watch, celebrations, critic fixes)
js/repertoire.data.js   GENERATED by tools/verify-repertoire.mjs from the selection — never hand-edit
js/facts.js             board facts of a move (capture, check, attacks, pins, development) — pure, tested
js/coach.js             coach voice: facts / threat / sourced idea → the line the coach says (text only)
js/ideas.data.js        GENERATED by tools/build-ideas.mjs — coach text for every shipped ply; never hand-edit
library/ideas.mjs       sourced per-move ideas (why + src + optional say/marks), keyed by SAN path
library/ideas-threats.json   cached engine null-move threats (tools/build-ideas.mjs)
library/ideas-cache/    git-ignored third-party text harvested by tools/fetch-ideas.mjs (studies, Wikibooks)
tools/fetch-ideas.mjs   harvest per-move comments from Lichess studies + Wikibooks, report coverage
tools/build-ideas.mjs   build js/ideas.data.js + docs/COACH.md
js/tree.js              pure helpers over the lines shape (repertoireMove, deviation, weighted pick)
js/progress.js          accuracy / streaks / weak spots, localStorage
js/settings.js          user settings, localStorage
js/feedback.js          square flash, board shake, haptics (sound off for now)
js/explorer.js          Lichess masters explorer client (needs a token)
js/chesscom.js          Chess.com game import + gap analysis
js/engine.js            Stockfish in a Web Worker, UCI wrapper, evaluate(sans)
js/branchBuilder.js     the eval-cutoff rule + line extender (pure, tested)
js/weakness.js          blunder finder over your own games + aggregation (tested)
sw.js                   service worker: offline + instant loads (bump VERSION whenever app-shell files change)
icon-192.png, icon-512.png   Android install icons (generated with Pillow)
tools/nodeEngine.mjs    the same Stockfish under Node, evaluate() contract as js/engine.js
library/lines.mjs            THE LIBRARY — single source of truth: every line with SAN, kind, weight, sources, note, plan
library/masters-cache.json   cached Lichess masters lookups used by build-library
library/explorer-cache.json  cached Lichess explorer nodes (masters / club 1400-1800 / strong 2200-2500) — the verifier's club-frequency check reads this offline
tools/explorer-cache.mjs     cached explorer client (reads the token from .env only for uncached positions; never prints it)
tools/human-tree.mjs         research: walk a tree of human moves (masters/strong at our nodes, club replies ≥ 15% at theirs) → JSON
tools/tree-leaves.mjs        render that JSON one leaf per line with provenance
tools/fetch-club.mjs         fetch + report club/strong frequency of every selected trap's mistake
tools/peek.mjs               show what the three databases say after a position
docs/LIBRARY.md              rendered library (✓ marks drilled lines) + masters' own tree (node tools/build-library.mjs)
docs/AUDIT.md                per drilled line: library id, weight, sources, engine plies (must be 0) (node tools/audit-repertoire.mjs)
tools/masters-tree.mjs       walk the masters DB from a root; branches with >= N games; example games
tools/explorer-check.mjs     cross-check the seed against masters + amateur frequencies
tools/repertoire.seed.mjs    THE SELECTION: which library lines the app drills, by id, with stable app ids — edit this
tools/verify-lib.mjs         structural rules + engine check as pure functions (tested with a fake engine)
tools/verify-repertoire.mjs  runs verify-lib with Stockfish; writes js/repertoire.data.js ONLY if nothing is flagged
tools/build-repertoire.mjs   research tool: what a user's opponents actually play (not shipped)
tools/build-branches.mjs     (older) rebuild single branches with the engine
tools/analyse-games.mjs      batch blunder analysis of a Chess.com user's games
vendor/chess.js         chess.js 1.0.0 ESM, BSD-2-Clause (licence beside it)
vendor/pieces/          cburnett SVG pieces, GPLv2+ (LICENSE.md beside them)
vendor/stockfish/       Stockfish 18 lite single-threaded WASM, 7 MB, GPL-3
tests/*.test.mjs        node:test suites for everything that is not DOM
docs/sources.md         every external URL consulted, dated
PLAN.md                 decisions, scope, job list — read first each session
PROJECT_BRIEF.md        the original spec and the "why"
```

## localStorage keys (all on the device that runs the app)

| Key | Content |
|---|---|
| `openingTrainer.progress.v1` | attempts, streaks, mistakes per opening |
| `openingTrainer.settings.v1` | sound, deviation chance, Lichess token, Chess.com username |
| `openingTrainer.gaps.v1` | last Chess.com scan report |
| `openingTrainer.weaknesses.v1` | last engine analysis of your games (positions to drill) |

Progress can be exported/imported as JSON from the Settings panel. That is
the backup. Everything else is regenerable.

## Run locally

```bash
npm run serve
```

then open http://127.0.0.1:8765/ . (Or use the Claude Code preview named
`static` in `.claude/launch.json`.) The app must be served over HTTP —
opening `index.html` directly from disk does not work because ES modules
are blocked on `file://`.

## Test

```bash
npm test
```

## Change the repertoire

1. Put the line in `library/lines.mjs` with exact SAN, `kind`, `weight`
   (A or B to be drillable), `sources`, a `note` saying which source covers
   which moves, and a `plan` + `planSources` (what to do when the line ends).
2. Select it in `tools/repertoire.seed.mjs` (`{ use: '<library id>', id: '<stable app id>' }`).
   Rules the verifier enforces: no engine moves, ends after our move, at
   least four of our moves after the deviation, no prefix duplicates, one
   move per position, only A/B weight.
3. Verify and regenerate the data file (fails closed — the file is written
   only when there is no flag):

```bash
node tools/verify-repertoire.mjs 18
```

4. Fix every STRUCTURE / DROP / SIDE? / REPEAT flag in the library or the
   selection and re-run. Then regenerate the docs and test:

```bash
node tools/audit-repertoire.mjs > docs/AUDIT.md
```

```bash
node tools/build-library.mjs "$TEMP/masters-scotch.json" "$TEMP/masters-elephant.json" --offline > docs/LIBRARY.md
```

```bash
npm test
```

## Put it on the phone

The phone needs to load the folder over HTTP once, then "Add to Home Screen"
in Chrome. Options, in order of least effort:

1. **Same Wi-Fi:** run `python -m http.server 8765` (without `--bind`) on the
   PC and open `http://<pc-ip>:8765/` on the phone. Exposes the folder on
   the LAN while running — fine for a static site with no personal data.
2. **Static host (in use since 2026-09-16):** the repo is public at
   https://github.com/stas1928-pixel/Cheka and served by GitHub Pages at
   **https://stas1928-pixel.github.io/Cheka/** from the `main` branch root.
   Publishing a change is just `git push`; Pages rebuilds within a minute.
   The repo contains no personal data; progress and tokens live only in
   the phone's localStorage.

Without a service worker the app still needs the network to load each
time; add one only if offline use becomes a real need (see PLAN.md).
The Stockfish engine (7 MB) downloads only when you first press
"Check with Stockfish", and the browser caches it afterwards.

### Same-Wi-Fi, step by step
1. On the PC, in the project folder, run `python -m http.server 8765`
   (no `--bind`, so the phone can reach it). Leave that window open.
2. Find the PC's address: `ipconfig` → "IPv4 Address", e.g. `192.168.1.23`.
3. On the phone (same Wi-Fi), open Chrome and go to
   `http://192.168.1.23:8765/` — your address instead of the example.
4. Chrome menu (⋮) → **Add to Home screen** → Add. The icon opens the app
   full-screen like a normal app.
5. Paste your Lichess token and Chess.com username into Settings on the
   phone — they are stored on the phone only.
6. Every time you want to use it, the PC server must be running. Stop it
   with Ctrl+C. If the PC's address changes, remove and re-add the icon.
Note: on a plain `http://` LAN address the Copy button in the engine
panel cannot use the clipboard; the JSON text is select-all on tap instead.

## Move to another machine

Copy the folder (keep `.git`, skip nothing else — there is no
`node_modules`). Run `npm test`. Export progress from the old device and
import on the new one if the phone changes.
